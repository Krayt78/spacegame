# Polkadot Hub TestNet configuration
# Usage: npm run dev:testnet

NEXT_PUBLIC_CHAIN=testnet
NEXT_PUBLIC_RPC_URL=http://127.0.0.1:8545
NEXT_PUBLIC_POLKADOT_HUB_RPC_URL=https://eth-rpc-testnet.polkadot.io
# Substrate WS endpoint for pallet-revive writes (must match the chain our
# contracts are deployed on — the SDK's "paseo" preset points at a different chain).
NEXT_PUBLIC_HUB_WS_URL=wss://asset-hub-paseo.dotters.network
NEXT_PUBLIC_NEXUS_GAME_ADDRESS=0xD2E15C81512238A3BFC38E37d4fb32B647f50450
NEXT_PUBLIC_GAME_CONFIG_ADDRESS=0x7F41b7fC8DBF2909A596E2751680efccb7d189F9

# DotNS identifier dot.li validates the product account against.
# MUST match the subdomain the dApp is deployed to. See useTriangle.ts.
NEXT_PUBLIC_DOT_NS_IDENTIFIER=nexusprotocol00.dot
